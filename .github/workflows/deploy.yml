name: Deploy

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      CLOUDFLARE_PAGES_PROJECT_NAME: ${{ secrets.CLOUDFLARE_PAGES_PROJECT_NAME }}
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_PROJECT_URL: ${{ secrets.SUPABASE_PROJECT_URL }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
      PUBLIC_BASE_URL: ${{ secrets.PUBLIC_BASE_URL }}
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      OPENDART_API_KEY: ${{ secrets.OPENDART_API_KEY }}
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Debug deployment inputs
        run: |
          set -euo pipefail
          echo "Node: $(node -v)"
          echo "pnpm: $(pnpm -v)"
          echo "supabase: $(supabase --version)"
          echo "wrangler: $(pnpm -C apps/bot-worker exec wrangler --version)"

          if [ -z "${SUPABASE_PROJECT_URL:-}" ]; then
            echo "SUPABASE_PROJECT_URL is missing"
            exit 1
          fi

          host="${SUPABASE_PROJECT_URL#https://}"
          host="${host#http://}"
          host="${host%%/*}"
          project_ref="${host%%.*}"

          runtime_host="${SUPABASE_URL#https://}"
          runtime_host="${runtime_host#http://}"
          runtime_host="${runtime_host%%/*}"
          runtime_ref="${runtime_host%%.*}"

          echo "Derived Supabase host: $host"
          echo "Derived Supabase project ref: $project_ref"
          echo "Derived runtime SUPABASE_URL host: $runtime_host"
          echo "Derived runtime SUPABASE_URL project ref: $runtime_ref"
          echo "Has SUPABASE_ACCESS_TOKEN: $([[ -n "${SUPABASE_ACCESS_TOKEN:-}" ]] && echo yes || echo no)"
          echo "Has SUPABASE_DB_PASSWORD: $([[ -n "${SUPABASE_DB_PASSWORD:-}" ]] && echo yes || echo no)"
          echo "Has SUPABASE_URL: $([[ -n "${SUPABASE_URL:-}" ]] && echo yes || echo no)"
          echo "Has SUPABASE_SECRET_KEY: $([[ -n "${SUPABASE_SECRET_KEY:-}" ]] && echo yes || echo no)"

          if [ -z "$project_ref" ] || [ -z "$runtime_ref" ]; then
            echo "Could not derive project refs from SUPABASE_PROJECT_URL or SUPABASE_URL"
            exit 1
          fi

          if [ "$project_ref" != "$runtime_ref" ]; then
            echo "SUPABASE_PROJECT_URL and SUPABASE_URL point to different projects."
            exit 1
          fi

      - name: Show migration files
        run: |
          set -euo pipefail
          ls -la infra/supabase
          ls -la infra/supabase/migrations

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build workspace
        run: pnpm -r --if-present build

      - name: Link Supabase project
        run: |
          set -euo pipefail
          host="${SUPABASE_PROJECT_URL#https://}"
          host="${host#http://}"
          host="${host%%/*}"
          project_ref="${host%%.*}"
          if [ -z "$project_ref" ]; then
            echo "Could not derive project ref from SUPABASE_PROJECT_URL"
            exit 1
          fi
          supabase --workdir infra/supabase link --project-ref "$project_ref" --password "$SUPABASE_DB_PASSWORD"

      - name: Show linked migration status
        run: |
          set -euo pipefail
          supabase --workdir infra/supabase migration list || true

      - name: Apply Supabase migrations
        run: |
          set -euo pipefail
          supabase --workdir infra/supabase db push

      - name: Verify subscriptions table exists
        run: |
          set -euo pipefail
          status_and_body="$(curl -sS -w '\n%{http_code}' "${SUPABASE_URL}/rest/v1/subscriptions?select=id&limit=1" \
            -H "apikey: ${SUPABASE_SECRET_KEY}" \
            -H "Authorization: Bearer ${SUPABASE_SECRET_KEY}" \
            -H "Accept: application/json")"

          http_status="$(printf '%s\n' "$status_and_body" | tail -n 1)"
          body="$(printf '%s\n' "$status_and_body" | sed '$d')"

          echo "Supabase subscriptions table probe HTTP status: $http_status"
          echo "$body"

          if [ "$http_status" -lt 200 ] || [ "$http_status" -ge 300 ]; then
            echo "subscriptions table probe failed"
            exit 1
          fi

      - name: Deploy bot worker
        run: |
          set -euo pipefail
          deploy_output="$(pnpm -C apps/bot-worker run deploy 2>&1)"
          printf '%s\n' "$deploy_output"

          worker_base_url="$(printf '%s\n' "$deploy_output" | python3 -c "import re,sys; s=sys.stdin.read(); m=re.search(r'https://[\\w.-]+\\.workers\\.dev', s); print(m.group(0) if m else '')")"

          if [ -n "$worker_base_url" ]; then
            echo "WORKER_BASE_URL=$worker_base_url" >> "$GITHUB_ENV"
            echo "Detected worker URL: $worker_base_url"
          else
            echo "Could not auto-detect worker URL from deploy output. Will fall back to PUBLIC_BASE_URL for health checks."
          fi

      - name: Deploy admin web
        run: |
          pages_project_name="${CLOUDFLARE_PAGES_PROJECT_NAME:-investment-helper-admin}"

          if ! pnpm -C apps/bot-worker exec wrangler pages project create "$pages_project_name" --production-branch main; then
            echo "Pages project create returned non-zero. It may already exist; continuing with deploy."
          fi

          if [ -d "apps/admin-web/.output/public" ]; then
            pnpm -C apps/bot-worker exec wrangler pages deploy ../admin-web/.output/public --project-name "$pages_project_name"
          elif [ -d "apps/admin-web/dist" ]; then
            pnpm -C apps/bot-worker exec wrangler pages deploy ../admin-web/dist --project-name "$pages_project_name"
          else
            echo "No deployable admin output found"
            exit 1
          fi

      - name: Post-deploy health checks
        run: |
          set -euo pipefail
          base_url="${WORKER_BASE_URL:-${PUBLIC_BASE_URL:-}}"

          if [ -z "$base_url" ]; then
            echo "Missing worker base URL. Set PUBLIC_BASE_URL secret to your Worker URL (e.g. https://<worker>.<subdomain>.workers.dev)."
            exit 1
          fi

          echo "Health check base URL: $base_url"
          curl -fsS -i "$base_url/health"
          curl -fsS -i "$base_url/admin/health"

      - name: Debug logs on failure
        if: failure()
        run: |
          set +e
          echo "==== wrangler logs directory ===="
          ls -la "$HOME/.config/.wrangler/logs" || true
